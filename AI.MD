# AI.MD - תיאור מבנה הפרויקט

זהו קובץ תיעוד שמסכם את מבנה הפרויקט ונותן למערכת AI מקום להתייחס אליו לפני חיפוש בכל הקבצים.

## מה יש בפרויקט

זהו פרויקט C++17 של משחק שחמט בזמן אמת בשם Kung-Fu Chess.

הפרויקט מחולק לשתי תוכניות עיקריות:

- `KungFuChessClient` - לקוח שמטפל בקלט משתמש, בתצוגה ובתקשורת WebSocket.
- `KungFuChessServer` - שרת שמנהל מצב משחק, מבצע חישובי חוקיות, ומקל על משחק בזמן אמת.

---

## קבצים עיקריים

| קובץ / ספריה | תיאור | תפקיד מרכזי |
|---|---|---|
| `CMakeLists.txt` | קובץ בנייה הראשי | מגדיר targets, includes ו-OpenCV |
| `README.md` | תיעוד כללי | מסביר מבנה, שכבות וארכיטקטורה |
| `Server_Design.MD` | מסמך עיצוב | פירוט עיצובי של שרת ומשחק |
| `board.txt` | קובץ מצב התחלתי | קובץ לוח טעינת התחלה |
| `client/` | קוד לקוח | קלט, תצוגה ותקשורת WebSocket |
| `server/` | קוד שרת | ניהול משחק, חוקיות, לולאת עדכון |
| `src/` | ליבת המשחק | מודל, תנועה, חוקיות, ארביטר ועוד |

---

## תיאור קבצים חשובים

### Root
- `CMakeLists.txt` - קובץ תצורה של CMake שמגדיר את כל ה-build targets, ספריות ההכללה, קישורים לספריות חיצוניות כמו OpenCV, והגדרות אזהרות.
- `README.md` - מסמך תיאור כללי של הפרויקט; מסביר את מבנה הספריות, האדריכלות והשכבות העיקריות.
- `Server_Design.MD` - פרטי עיצוב של השרת; מציג החלטות עיצוב, ארכיטקטורה של השרת וניצול חלקי המערכת.
- `board.txt` - קובץ התחלת לוח שחמט שמיטען בשרת בזמן יצירת משחק, כנראה מספק מצב התחלתי לקליטת תנועות.

### client/
- `main.cpp` - נקודת הכניסה ליישום הלקוח; מאתחל את הממשק למשתמש, מייצר את חיבור ה-WebSocket ומתחיל את זרימת העבודה.
- `NetworkClient.cpp` / `NetworkClient.hpp` - מנהלים את החיבור לשרת, שליחת הודעות ופרוטוקול תקשורת לקליטת עדכוני מצב.

### server/
- `main.cpp` - נקודת הכניסה ליישום השרת; יוצרת `GameManager`, טוענת את `board.txt`, מגדירה את `WebSocketServer` ואת `GameLoop` ומפעילה אותם.
- `WebSocketServer.cpp` / `WebSocketServer.hpp` - מנהלים חיבורים מלקוחות, קבלת הודעות וכיוון של שידורי מצב ללקוחות.
- `GameLoop.cpp` / `GameLoop.hpp` - לולאת המשחק הראשית; מרעננת מצב משחק, מפעילה עדכונים ומבצעת callback לשליחת snapshot ללקוח.
- `GameManager.hpp` / `GameManager.cpp` - מנהל את מצב המשחקים הפעילים בשרת, יצירת משחקים, טעינת לוח ותחזוקת מצבים.
- `PlayerSession.hpp` - מייצג חיבור משתמש יחיד בשרת, כולל מידע על הסשן והקשר הרשת.

### src/
- `src/model/` - מכיל את מודל המשחק הבסיסי: ייצוג לוח, כלים, מצבי משחק ויצירת כלים.
- `src/movement/` - מגדיר חוקי תנועה לכלים: כל כלי מקבל את חוקי התנועה שלו.
- `src/rule_engine/` - מבצע בדיקות חוקיות תנועה, בדיקות גבולות, תקפות נתיב ושמירה על כללי המשחק.
- `src/arbiter/` - לוגיקה של זמן אמת לניהול תנועות, פתרון קונפליקטים ומעקב אחרי מצב המשחק במהלך עשייה.
- `src/game_engine/` - תיאום בין כל מרכיבי המשחק; חיבור לוגיקת מודל, תנועה, rule engine וארביטר.
- `src/network/` - מגדיר פרוטוקולים, מבני הודעות וסיריאליזציה/דסיריאליזציה לנתונים שרץ ברשת.
- `src/renderer/` - לוגיקה להצגת המשחק בלקוח, אפשר שימוש ב-OpenCV לרינדור ותמונות.
- `src/controllerClick/` - טיפול באירועי לחיצה ובקרת קלט המשתמש בממשק.
- `src/audio/` - ניהול וקלט של סאונד וקולות במשחק.
- `src/config/` - קבצי קונפיגורציה והגדרות של הפרויקט.
- `src/text_io/` - טיפול בקלט/פלט טקסטואלי, ייתכן לשימוש בממשק טקסט או בבדיקות.
- `src/commands/` - יישום פקודות משחק ומדיניות ביצוע פעולות.

---

## ספריות מרכזיות

### `client/`
מכילה את קוד הלקוח:
- `main.cpp` - נקודת הכניסה ללקוח.
- `NetworkClient.cpp` / `.hpp` - תקשורת WebSocket עם השרת.

### `server/`
מכילה את קוד השרת:
- `main.cpp` - נקודת הכניסה לשרת.
- `WebSocketServer.cpp` / `.hpp` - שרת WebSocket לשידור עדכונים.
- `GameLoop.cpp` / `.hpp` - לולאת המשחק הראשית שמעדכנת מצב ומשדרת ללקוחות.
- `GameManager.*` - ניהול משחקים וטעינת לוח.

### `src/`
שכבת הליבה של המשחק עם מחלקות מודל, תנועה, חוקיות, ארביטר ותקשורת.

#### תת-ספריות חשובות
- `src/model/` - מודל משחק: לוח, כלי, יצירת כלים.
- `src/movement/` - חוקי תנועה עבור כלי שחמט.
- `src/rule_engine/` - בדיקת חוקיות תנועות.
- `src/arbiter/` - לוגיקה של אלגוריתם זמן אמת וניהול תנועות.
- `src/game_engine/` - חיבור כל חלקי המשחק יחד.
- `src/network/` - כלי תקשורת והעברת הודעות.
- `src/renderer/`, `src/controllerClick/`, `src/audio/` - לוגיקות לקוח ותצוגה.

---

## איך להשתמש ב-AI.MD

בבקשה תוכל להגיד ל-AI:

- "תסתכל ב-AI.MD" לפני שאתה מבצע ניתוח של הקוד.
- "פתח את AI.MD" כשאתה מחפש מבנה מהיר של הפרויקט.

### למה זה חשוב

הקובץ הזה מאפשר לפשט את זיהוי המבנה והכיוונים העיקריים בפרויקט בלי לקרוא את כל הקבצים בכל פעם.

---

## נקודות מפתח

- בנייה מתבצעת באמצעות `CMake`.
- יש שני targets: `KungFuChessClient` ו-`KungFuChessServer`.
- משתמש ב-OpenCV לעיבוד תמונה/הצגה בלקוח.
- תקשורת מבוססת WebSocket בין לקוח לשרת.
- החלקים העיקריים הם מודל, תנועה, חוקיות, ארביטר ולולאת המשחק.
