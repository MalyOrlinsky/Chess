# AI.MD - תיעוד מבנה הפרויקט
קובץ זה מסכם את מבנה הפרויקט ועוזר להבין את הזרימה הכללית לפני קריאת קוד.

## מה זה הפרויקט

Kung-Fu Chess הוא משחק שחמט בזמן אמת ב־C++17 עם שרת ולקוח.
הפרויקט משתמש ב־WebSocket לתקשורת בין הלקוח לשרת וב־OpenCV בצד הלקוח להצגת המשחק.

### Targets מרכזיים

- `KungFuChessClient` – לקוח אינטראקטיבי.
- `KungFuChessServer` – שרת משחק.

---

## נקודות כניסה חשובות

- `CMakeLists.txt` – מגדיר את שני ה־targets, קישורי OpenCV ו־ספריות חיצוניות.
- `README.md` – הוראות בנייה והרצה ומבנה פרויקט כללי.
- `Server_Design.MD` – תיעוד עיצוב שרת וזרימת המשחק בצד השרת.
- `AI.MD` – מסמך מבנה מהיר זה.
- `board.txt` – מצב התחלה של לוח השחמט.

---

## ספריות גדולות

| ספריה | תפקיד עיקרי |
|---|---|
| `client/` | קוד לקוח: התחברות לשרת, UI ו־WebSocket |
| `server/` | קוד שרת: ניהול משחק, חדרים, matchmaking ותקשורת |
| `src/` | ליבת המשחק: מודל, תנועה, חוקיות, ארביטר, פקודות ועוד |
| `opencv2/` | OpenCV מקומי ו־img renderer לצד הלקוח |
| `external/` | ספריות חיצוניות: WebSocket++, ASIO, nlohmann/json |
| `assets/` | תמונות וסאונד |
| `tests/` | בדיקות ותסריטי בדיקה |

---

## זרימת תקשורת וארכיטקטורה

1. הלקוח מחבר לשרת דרך WebSocket.
2. הלקוח שולח פקודות, כניסה ופעולת Play/Room.
3. השרת מקבל הודעות ב־`WebSocketServer`.
4. השרת מפנה את ההודעות ל־`MatchmakingManager` או `RoomManager`.
5. כשהמשחק מתחיל, `GameManager` מנהל משחקים פעילים.
6. `GameLoop` מונע עדכוני מצב ותזמון.
7. `GameEngine` ו־`RuleEngine` בודקים את חוקיות המהלכים.
8. השרת שולח עדכוני מצב חזרה ללקוח.

---

## תיאור רכיבים חשובים

### client/

* `main.cpp` – תחילת הלקוח.
* `NetworkClient.*` – חיבור WebSocket, שליחה וקבלת הודעות, ניהול סנאפשוט ומשחק.
* `src/renderer/` – רינדור המסך.
* `opencv2/src/img.cpp` – קוד תמיכה ב־OpenCV להצגת תמונה.

### server/

* `main.cpp` – תחילת השרת.
* `WebSocketServer.*` – קבלת הודעות מלקוחות ושליחת עדכונים.
* `MatchmakingManager.*` – ניהול משחקי matchmaking וסגירת זוגות.
* `RoomManager.*` – ניהול חדרים פרטיים, הצטרפות צופים ושידור עדכונים.
* `GameManager.*` – ניהול המשחקים הפעילים ויצירת מופעי `Game`.
* `GameLoop.*` – לולאת משחק רציפה לטיפול בהתפתחויות בזמן אמת.
* `Game.*` – לוגיקת משחק יחיד.
* `PlayerSession.hpp` – ייצוג חיבור משתמש.

### src/

* `src/model/` – מודל המשחק: לוח, כלי, מצב משחק.
* `src/movement/` – חוקי תנועה לכל כלי.
* `src/rule_engine/` – אימות חוקיות מהלכים.
* `src/arbiter/` – לוגיקה של זמן אמת, ניהול תנועה ועמידה בהתנגשויות.
* `src/game_engine/` – שילוב בין מודל, חוקיות ושרת.
* `src/network/` – פרוטוקול הודעות וסיריאליזציה.
* `src/commands/` – מימוש פקודות משחק.
* `src/controllerClick/` – עיבוד קלט לחיצות.
* `src/audio/` – ניהול סאונד.
* `src/config/` – קוד קונפיגורציה משותף.
* `src/text_io/` – קלט ו־פלט טקסטואלי.

---

## מה חשוב לדעת

* הצד הלקוח עושה שימוש ב־OpenCV להצגת המשחק.
* תצורת בנייה מוגדרת ב־`CMakeLists.txt`.
* יש הפרדה בין ניהול תקשורת (`WebSocketServer`), חדרים (`RoomManager`) ו־matchmaking (`MatchmakingManager`).
* `GameManager` אחראי על יצירת וניהול מופעי משחק בשרת.
* `RoomManager` מנהל משתתפים וחדרים; הוא אינו אחראי על לוגיקת המשחק עצמה.
* `MatchmakingManager` מקשר שחקנים ל־Play ומתחיל את המשחק.

---

## קישורים מהירים

* `README.md` – הוראות בנייה והרצה.
* `Server_Design.MD` – עיצוב שרת.
* `CMakeLists.txt` – קביעת תצורת הבנייה.

---

## המלצות לשימוש

- קראו את `AI.MD` לפני חיפוש קבצים כדי להבין מה נמצא בכל ספרייה.
- אם אתם עובדים על צד הלקוח, התחילו ב־`client/` וב־`src/renderer/`.
- אם אתם עובדים על הלוגיקה של המשחק, עברו ל־`src/model/`, `src/rule_engine/` ו־`src/arbiter/`.
- אם אתם עובדים על ניהול חיבור או חדרים, עברו ל־`server/WebSocketServer.*`, `server/MatchmakingManager.*` ו־`server/RoomManager.*`.
anager אינו מנהל את המשחק עצמו אלא רק את המשתמשים בחדר.
- MatchmakingManager אחראי ליצירת משחקים ולהקצאת צבעים.