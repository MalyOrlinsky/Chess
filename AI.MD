# AI.MD - תיעוד מבנה הפרויקט

קובץ זה מסכם את מבנה הפרויקט ומשמש כמפת רעיונות ל־AI לפני חיפוש או ניתוח של קבצים.

## מה יש בפרויקט

זהו פרויקט C++17 של משחק שחמט בזמן אמת בשם Kung-Fu Chess עם לקוח ושרת.

### Targets עיקריים

- `KungFuChessClient` - לקוח שמטפל בקלט משתמש, תצוגה ותקשורת WebSocket.
- `KungFuChessServer` - שרת שמנהל מצב משחק, חוקיות, לולאת עדכון ומשדר לקוחות.

---

## קבצים וספריות מרכזיות

| קובץ / ספריה | תיאור | תפקיד מרכזי |
|---|---|---|
| `CMakeLists.txt` | קובץ בנייה ראשי | מגדיר targets, includes, OpenCV ו־WebSocket++ |
| `README.md` | תיעוד כללי | מסביר מבנה הפרויקט, ארכיטקטורה והוראות build/run |
| `Server_Design.MD` | מסמך עיצוב שרת | החלטות ארכיטקטורה וזרימת משחק בצד השרת |
| `board.txt` | לוח התחלה | מצב התחלתי של לוח שחמט לטעינה |
| `client/` | קוד לקוח | קלט, רינדור ותקשורת WebSocket |
| `server/` | קוד שרת | לולאת משחק, ניהול משחקים, WebSocket ו־Session |
| `src/` | ליבת המשחק | מודל, תנועה, חוקיות, ארביטר, פקודות, רשת ועוד |
| `opencv2/` | קבצי OpenCV ו־img renderer | ספרייה מקומית של OpenCV וקוד רינדור תמונה |
| `external/` | ספריות חיצוניות | websocketpp, asio, nlohmann/json |
| `assets/` | משאבים | תמונות וסאונד |
| `tests/` | בדיקות | בדיקות יחידה וסקריפטים |

---

## תיאור רכיבים חשובים

### Root
- `CMakeLists.txt` - קובץ תצורה שמגדיר את בניית הלקוחות והשרת, כולל קישורים ל־OpenCV ולספריות חיצוניות.
- `README.md` - מסמך תיאור ראשי של הפרויקט.
- `Server_Design.MD` - תיעוד עיצובי של שרת המשחק.
- `board.txt` - קובץ התחלת לוח שחמט.

### `client/`
- `main.cpp` - נקודת כניסה ליישום הלקוח.
- `NetworkClient.cpp` / `NetworkClient.hpp` - מנהלים חיבור WebSocket לשרת ועיבוד הודעות.

### `server/`
- `main.cpp` - נקודת כניסה לשרת.
- `WebSocketServer.cpp` / `WebSocketServer.hpp` - ניהול חיבורים, קבלת הודעות ושידור עדכוני מצב.
- `GameLoop.cpp` / `GameLoop.hpp` - לולאת המשחק הראשית.
- `GameManager.*` - ניהול משחקים פעילים, סשנים וטעינת מצב.
- `PlayerSession.hpp` - ייצוג סשן משתמש יחיד.
 - `Game.cpp` / `Game.hpp` - מימוש מחלקת `Game` (מצב לוח, עדכוני משחק, לוגיקת משחק).

### `src/`
- `src/model/` - מודל משחק: לוח, כלי וניהול מצב משחק.
- `src/movement/` - חוקי תנועה לכל כלי שחמט.
- `src/rule_engine/` - בדיקת תקינות מהלכים וחוקיות.
- `src/arbiter/` - לוגיקת זמן אמת, ניהול תנועה וקונפליקטים.
- `src/game_engine/` - תיאום בין מודל, תנועה, חוקיות ושרת.
- `src/network/` - פרוטוקול הודעות וסיריאליזציה.
- `src/renderer/` - רינדור מצב המשחק בצד הלקוח.
- `src/controllerClick/` - קלט לחיצות ועיבוד ממשק.
- `src/audio/` - ניהול אפקטי סאונד.
- `src/config/` - קוד קונפיגורציה משותף.
- `src/text_io/` - טיפול בקלט/פלט טקסטואלי.
- `src/commands/` - פקודות משחק והפעלות.

---

## מה שמיוחד בפרויקט

- בנייה מבוססת `CMake`.
- שני targets ברורים: `KungFuChessClient` ו־`KungFuChessServer`.
- שימוש ב־OpenCV עבור צד הלקוח.
- תקשורת WebSocket בין הלקוח לשרת.
- יש הפרדה בין לוגיקת המודל, חוקיות התנועה, זמן אמת והרינדור.
- `src/config/` משמש כקוד משותף בין הלקוח והשרת.

---

## איך להשתמש ב־AI.MD

בקש מה־AI:

- "הסתכל ב־AI.MD" כדי לקבל מבנה מהיר של הפרויקט.
- "פתח את AI.MD" כשאתה צריך לדעת אילו ספריות עיקריות קיימות.

### למה זה חשוב

הקובץ מקצר את הזמן שלוקח להבין את המבנה הכולל של הפרויקט לפני קריאת קוד ספציפי.

---

## נקודות למעקב

- יש קבצי `client/`, `server/`, `src/`, `external/` ו־`opencv2/`.
- קבצים חשובים לשם בנייה והרצה נמצאים ב־`CMakeLists.txt`.
- כל צד משתמש ב־OpenCV רק בצד הלקוח.
- השרת בונה גם את מודול `src/controllerClick/`, כנראה לשימוש לוגי בשרת.
- המודולים העיקריים כוללים מודל, תנועה, חוקיות, ארביטר, ולולאת משחק.
